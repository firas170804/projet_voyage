{% extends 'base.html.twig' %}

{% block title %}Aeroport{% endblock %}

{% block body %}
    <h1>Aeroport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aeroport.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ aeroport.nom }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ aeroport.ville }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ aeroport.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_aeroport_index') }}">back to list</a>

    <a href="{{ path('app_aeroport_edit', {'id': aeroport.id}) }}">edit</a>

    {{ include('aeroport/_delete_form.html.twig') }}
{% endblock %}
