<nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top py-3 shadow-sm" data-navbar-on-scroll="data-navbar-on-scroll">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
      <img class="d-inline-block" src="{{ asset('deseign1/img/gallery/logo.png') }}" width="50" alt="logo" />
      <span class="fw-bold text-primary ms-2">voyage</span>
    </a>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mx-auto pt-2 pt-lg-0">
        <li class="nav-item px-2">
          <a class="nav-link" href="{{ path('app_vol_index') }}">
            <i class="fas fa-plane me-1"></i>
            <span class="nav-link-text">Flights</span>
          </a>
        </li>
        <li class="nav-item px-2">
          <a class="nav-link" href="{{ path('app_reservation_index') }}">
            <i class="fas fa-ticket-alt me-1"></i>
            <span class="nav-link-text">Reservation</span>
          </a>
        </li>
      </ul>
      
      <ul class="navbar-nav">
        <li class="nav-item px-2">
          <a class="nav-link" href="#">
            <i class="fas fa-user-circle me-1"></i>
            <span class="nav-link-text">Account</span>
          </a>
        </li>
        <li class="nav-item px-2">
          <a class="nav-link" href="{{ path('app_logout') }}">
            <i class="fas fa-sign-out-alt me-1"></i>
            <span class="nav-link-text">Logout</span>
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<!-- Espace après la navbar -->
<div style="height: 80px;"></div>

<style>
  /* Styles améliorés pour la navbar */
  .navbar {
    transition: all 0.3s ease;
    border-bottom: 1px solid rgba(0,0,0,0.1);
  }
  
  .nav-link {
    color: #2d3748 !important;
    padding: 8px 15px;
    border-radius: 8px;
    transition: all 0.2s ease;
    font-weight: 500;
  }
  
  .nav-link:hover {
    background-color: rgba(66, 133, 244, 0.1);
    color: #4285f4 !important;
  }
  
  .nav-link i {
    width: 20px;
    text-align: center;
  }
  
  /* Correction pour le contenu fixe */
  body {
    padding-top: 80px;
  }
  
  @media (max-width: 992px) {
    .navbar-collapse {
      padding-top: 20px;
      border-top: 1px solid rgba(0,0,0,0.1);
    }
    
    .nav-item {
      margin-bottom: 10px;
    }
  }
</style>